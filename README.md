# 판재 주문 계산기 (외부 공개용)

- `index.html` : 고객이 품목을 고르면 총액(공급가·부가세·합계)이 나오고, 견적 요청서를 복사/공유할 수 있는 페이지
- `stock.json` : Notion 「외부 공개 재고」에서 자동으로 뽑은 품목·판매가·현재고 (매시 05분 갱신)
- 갱신은 Actions → "재고 갱신(stock.json)" 에서 수동 실행도 가능. Secrets 에 `NOTION_TOKEN` 필요.
